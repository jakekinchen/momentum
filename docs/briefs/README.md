# Briefs

This folder stores per-slice implementation briefs from the autonomous workflow.

Briefs are useful historical context for what a slice intended to do, but they are not automatically current product direction. When in doubt, prefer:

1. The active `GOAL.md`.
2. Canonical docs under [`../design/`](../design/).
3. Current code and tests.
4. The relevant brief for slice-level context.
