# Requirements

This folder stores external requirements and golden data snapshots used as source material for CamiFit planning.

## Candidate assessment

[`candidate-assessment/`](candidate-assessment/) is a vendored snapshot of the Future Research AI Engineer take-home:

- `ASSESSMENT.md` - full requirements spec.
- `data/exercises.json` - 50-exercise golden catalog.
- `data/member-context.json` - synthetic member context for Jordan Rivera.
- `PROVENANCE.md` - source URL, upstream commit, and snapshot notes.

Treat this folder as a golden spec. If the upstream assessment changes, refresh the snapshot intentionally and update `PROVENANCE.md`.
