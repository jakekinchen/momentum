# Goal Loop Ledger

Objective:
Build the first concrete slice of the Momentum/CamiFit motion-data factory so bad exercise motion cannot be silently promoted from "detector produced landmarks" to "product-trustworthy motion." The loop should turn `docs/research/2026-06-23-motion-data-pipeline-deep-dive.md` into executable repo scaffolding, validation/reporting, and review guidance.

Started:
2026-06-23

Cadence:
Iterate locally until either the done criteria pass or a hard blocker repeats for three materially identical iterations.

Owner constraints:
- Work only in `/Users/kelly/Developer/camifit-app-agent-motion-data-factory`.
- Target branch is `agent/motion-data-factory-goal-loop`.
- Base branch is `main` at `f5b2c30`.
- The live checkout at `/Users/kelly/Developer/camifit-app` has unrelated dirty files. Do not touch, restore, overwrite, or depend on that checkout.
- Do not delete or rename `/Users/kelly/Developer/camifit` or `/Users/kelly/Developer/camifit-pose`.
- Do not promote any `reference_capture_required` exercise to guide-ready.
- Do not modify packaged `Sources/CamiFitApp/Resources/MotionDemos/*.jsonl` unless the change is only to add explicit non-promotional metadata or tests prove it is required.

Authorized mutations:
- `scripts/motion_reference/**`
- `docs/research/**`
- `docs/manual-verification/**`
- `website/src/app/motion-review/**`
- `website/src/lib/motionReview.ts`
- focused tests under `scripts/motion_reference/test_*.py`

## Done Criteria

- A repo-local motion-data factory preflight exists and can classify exercises into explicit promotion tiers, including at least:
  - recommendation-only
  - source-candidate
  - detector-reviewable
  - avatar-demo-candidate
  - guide-ready
  - validation-ready
- The preflight has hard, machine-readable reasons for why an exercise is not guide-ready or validation-ready.
- The preflight does not merely duplicate `report_motion_pipeline_gaps.py`; it must add at least one new factory concept from the deep-dive, such as capture session metadata, detector agreement scorecards, kinematic scorecards, or explicit human visual-review decisions.
- Any new schemas, examples, or scorecard contracts are documented and covered by tests.
- Existing guide-ready exercises remain guide-ready unless a real discovered defect is documented as a blocker.
- No bad or pending visual-review exercise is promoted.
- All relevant local proof commands pass, or the loop exits blocked with exact failing command output and a minimal unblock plan.

## Proof Target

- Surface:
  motion-reference pipeline scripts, tests, and review docs.
- Required commands:
  - `python3 -m py_compile scripts/motion_reference/*.py`
  - `python3 scripts/motion_reference/test_report_motion_pipeline_gaps.py`
  - any new `scripts/motion_reference/test_*.py` added by this work
  - `python3 scripts/motion_reference/report_motion_pipeline_gaps.py`
  - the new preflight/report command added by this loop
- Required result:
  all commands exit 0, and the new report exposes actionable tier/reason output for the current 15 exercise rows.

## Current State

- Last proof:
  Parent thread generated `tmp/motion-review-deep-dive/gap-report.json`.
- Last result:
  Current report shows 15 exercise rows, 4 playable JSONLs, 4 guide-ready IDs, 11 reference-capture-required IDs, 4 blocked visual-review rows, and 4 guide-ready traces relying on local-only `dist/` source-chain artifacts.
- Known blockers:
  Actual new first-party video capture cannot happen inside this loop unless source files already exist locally. If capture is required, exit with a capture checklist and exact next capture pack contract instead of fabricating data.

## Iterations

| Time | Action | Proof | Result | Next Step |
| --- | --- | --- | --- | --- |
| 2026-06-23 16:07 CDT | Read AGENTS.md, GOAL.md, the motion-data deep dive, motion-reference README, existing gap/audit scripts, and confirmed the isolated worktree/branch. | `git status --short --branch`; `git worktree list --porcelain`; source reads of `report_motion_pipeline_gaps.py`, `audit_motion_coverage.py`, `audit_kg_motion_readiness.py`, manifests, and profile registry. | Target worktree is `/Users/kelly/Developer/camifit-app-agent-motion-data-factory` on `agent/motion-data-factory-goal-loop` at `f5b2c30`; only `GOAL.md` is modified as the loop ledger. Current app gate has 4 guide-ready IDs and 11 reference-capture-required IDs; the first slice should reuse the gap/audit facts while adding factory-specific capture/review/scorecard gates. | Add a repo-local motion-data factory preflight/report, JSON schema contracts, focused tests, and docs without modifying packaged MotionDemos. |
| 2026-06-23 16:13 CDT | Added `preflight_motion_data_factory.py`, scorecard/capture/report schemas, unit tests, README docs, and manual verification guidance. | `python3 -m py_compile scripts/motion_reference/*.py`; `python3 scripts/motion_reference/test_preflight_motion_data_factory.py`; `python3 scripts/motion_reference/preflight_motion_data_factory.py`. | Initial proof passed. Preflight reports 15 exercise rows, 4 guide-ready, 0 validation-ready, 6 source-candidate, 5 avatar-demo-candidate, and 11 blocked from guide-ready. Existing guide-ready IDs remain guide-ready; reference-capture-required rows stay below guide-ready. | Run the full GOAL proof target, fix any failures, then commit locally if all done criteria pass. |
| 2026-06-23 16:15 CDT | Ran the full proof target and self-audit pass. | `python3 -m py_compile scripts/motion_reference/*.py`; `python3 scripts/motion_reference/test_report_motion_pipeline_gaps.py`; `python3 scripts/motion_reference/test_preflight_motion_data_factory.py`; `python3 scripts/motion_reference/report_motion_pipeline_gaps.py`; `python3 scripts/motion_reference/preflight_motion_data_factory.py`; `scripts/motion_reference/preflight_motion_data_factory.py`; `git diff --check`. | All proof commands exited 0. Existing gap report still shows 4 guide-ready and 11 reference-capture-required rows; new factory preflight exposes 15 tiered rows, 4 guide-ready, 0 validation-ready, and 11 guide blockers. No packaged `Sources/CamiFitApp/Resources/MotionDemos/*.jsonl` files changed. | Commit the completed executable slice locally on `agent/motion-data-factory-goal-loop` and report the hash. |
| 2026-06-24 12:49 CDT | Added a gallery asset generator for the next reviewable slice after the Vercel gallery exposed missing media coverage. | `python3 -m py_compile scripts/motion_reference/*.py`; `python3 scripts/motion_reference/test_report_motion_pipeline_gaps.py`; `python3 scripts/motion_reference/test_preflight_motion_data_factory.py`; `python3 scripts/motion_reference/test_generate_motion_review_gallery_assets.py`; `python3 scripts/motion_reference/report_motion_pipeline_gaps.py`; `python3 scripts/motion_reference/preflight_motion_data_factory.py --json-output tmp/motion-data-factory-preflight.json --markdown-output tmp/motion-data-factory-preflight.md`; `npm run lint`; `npm run build`; Playwright local desktop/mobile smoke on `http://127.0.0.1:3011/motion-review`. | Four public skeleton review MP4s are packaged for the four existing JSONL traces and the gallery snapshot reports 4 review videos. The other 11 exercises remain blocked by missing playable JSONL/reference capture/source review data; no packaged MotionDemos JSONL files changed. | Commit, push to `main`, deploy the website, and verify the live `/motion-review` route. |
| 2026-06-24 14:21 CDT | Added the next factory slice: path-backed visual review support, source/capture/review evidence in the gallery, and reusable first-party capture/review templates for the next target exercises. | `python3 -m py_compile scripts/motion_reference/*.py`; `python3 scripts/motion_reference/test_report_motion_pipeline_gaps.py`; `python3 scripts/motion_reference/test_preflight_motion_data_factory.py`; `python3 scripts/motion_reference/test_generate_motion_review_gallery_assets.py`; `python3 scripts/motion_reference/report_motion_pipeline_gaps.py`; `python3 scripts/motion_reference/preflight_motion_data_factory.py --json-output tmp/motion-data-factory-preflight.json --markdown-output tmp/motion-data-factory-preflight.md`; `npm run lint`; `npm run build`; Playwright local desktop/mobile smoke on `http://127.0.0.1:3012/motion-review`. | Proof still reports 15 rows, 4 guide-ready, 0 validation-ready, and 11 blocked from guide-ready. The gallery now exposes source evidence, capture-session evidence, and visual-review status without promoting any blocked exercise or editing packaged MotionDemos JSONL files. | Commit and push the evidence-contract slice, then deploy the updated gallery if the user wants the live site refreshed. |
| 2026-06-24 14:32 CDT | Added the capture-intake slice for the first real target pass: `register_motion_capture_session.py`, capture registration tests, docs, and gallery capture-plan visibility for the three next exercises. | `python3 -m py_compile scripts/motion_reference/*.py`; `python3 scripts/motion_reference/test_preflight_motion_data_factory.py`; `python3 scripts/motion_reference/test_report_motion_pipeline_gaps.py`; `python3 scripts/motion_reference/test_generate_motion_review_gallery_assets.py`; `python3 scripts/motion_reference/test_register_motion_capture_session.py`; `python3 scripts/motion_reference/report_motion_pipeline_gaps.py`; `python3 scripts/motion_reference/preflight_motion_data_factory.py --json-output tmp/motion-data-factory-preflight.json --markdown-output tmp/motion-data-factory-preflight.md`; `npm run lint`; `npm run build`; Playwright local desktop/mobile smoke on `http://127.0.0.1:3013/motion-review`. | The intake tool copies real source files into ignored `dist/`, writes `capture_session.json`, writes pending `visual_review.json`, and emits a manifest patch without promotion. The gallery now shows `bodyweight_plank` as capture priority #1. Counts remain 4 guide-ready, 0 validation-ready, 11 blocked. | Commit, push, deploy, and then capture/register real plank side/front source files outside Codex. |
| 2026-06-24 14:44 CDT | Reworked the first target pass for self-sourced data: downloaded and verified the licensed Pexels plank source, registered it as an external capture session, embedded non-promotional capture metadata in the plank manifest, and made the gallery use the external media URL instead of a local-only `dist/` route. | `curl -I https://videos.pexels.com/video-files/7801720/7801720-uhd_2732_1154_25fps.mp4`; `shasum -a 256 dist/motion-reference/bodyweight_plank/source/a-woman-doing-plank-exercise-on-a-yoga-mat-7801720.mp4`; `python3 -m py_compile scripts/motion_reference/*.py`; `python3 scripts/motion_reference/test_register_motion_capture_session.py`; `python3 scripts/motion_reference/test_preflight_motion_data_factory.py`; `python3 scripts/motion_reference/test_report_motion_pipeline_gaps.py`; `python3 scripts/motion_reference/test_generate_motion_review_gallery_assets.py`; `python3 scripts/motion_reference/report_motion_pipeline_gaps.py`; `python3 scripts/motion_reference/preflight_motion_data_factory.py --json-output tmp/motion-data-factory-preflight.json --markdown-output tmp/motion-data-factory-preflight.md`; `npm run lint`; `npm run build`; Playwright local desktop/mobile smoke on `http://127.0.0.1:3014/motion-review`. | Source media is verified at 12,055,907 bytes with sha256 `134712b11aaf6b939b3b5e78e193271e30cc7b255b0a25bd568e2bd0140dc116`. Gallery shows plank source and `licensed_external_clip` capture metadata, but plank remains `avatar-demo-candidate`, not guide-ready, not validation-ready, and blocked by failed visual review plus missing detector/kinematic/runtime validation artifacts. | Commit, push to `main`, deploy, then run detector extraction and scorecards from the sourced plank clip before any promotion. |
| 2026-06-24 15:01 CDT | Ran the sourced plank clip through MediaPipe, regenerated the normalized static-hold trace, added reusable scorecard generation, embedded plank detector/kinematic scorecards, and packaged the plank detector review MP4 for the gallery. | `CAMIFIT_PYTHON=/Users/kelly/Developer/camifit-app/.venv/bin/python scripts/motion_reference/export_mediapipe_reference_trace.py --video dist/motion-reference/bodyweight_plank/source/a-woman-doing-plank-exercise-on-a-yoga-mat-7801720.mp4 --exercise-id bodyweight_plank --output-dir dist/motion-reference/bodyweight_plank/pexels_plank_7801720_0_6 --fps 10 --start-ms 0 --end-ms 6000 --python /Users/kelly/Developer/camifit-app/.venv/bin/python --model /Users/kelly/Developer/camifit-app/pose_worker/models/pose_landmarker_lite.task`; `python3 scripts/motion_reference/normalize_plank_trace.py ...`; `python3 scripts/motion_reference/score_motion_reference_trace.py ...`; `python3 scripts/motion_reference/render_mediapipe_trace_review.py ...`; `python3 -m py_compile scripts/motion_reference/*.py`; focused motion-reference tests including `test_score_motion_reference_trace.py`; `python3 scripts/motion_reference/report_motion_pipeline_gaps.py`; `python3 scripts/motion_reference/preflight_motion_data_factory.py --json-output tmp/motion-data-factory-preflight.json --markdown-output tmp/motion-data-factory-preflight.md`; `npm run lint`; `npm run build`; Playwright local mobile smoke on `http://127.0.0.1:3015/motion-review`. | Raw MediaPipe extraction produced 60/60 posed frames with frame coverage 1.0, mean visibility 0.81981, identity flips 0, temporal jitter 0.017439. Detector scorecard remains failed because only MediaPipe was used, so true detector agreement is not proven. Kinematic scorecard passed with plank line 161.261595 degrees, loop/contact deltas 0, and phase monotonicity 1.0. Gallery detector reviews increased to 5. Counts remain 4 guide-ready, 0 validation-ready, 11 blocked. | Commit, push to `main`, deploy, then add either a second detector/comparator or a human side-by-side visual rig review before considering plank promotion. |
| 2026-06-24 15:24 CDT | Packaged the plank side-by-side detector review MP4 and taught the website/snapshot generator to prefer it over the legacy skeleton-only review video when available. | `python3 -m py_compile scripts/motion_reference/generate_motion_review_gallery_assets.py scripts/motion_reference/score_motion_reference_trace.py scripts/motion_reference/preflight_motion_data_factory.py scripts/motion_reference/report_motion_pipeline_gaps.py scripts/motion_reference/register_motion_capture_session.py`; focused motion-reference tests including `test_generate_motion_review_gallery_assets.py`; `python3 scripts/motion_reference/report_motion_pipeline_gaps.py`; `python3 scripts/motion_reference/preflight_motion_data_factory.py --json-output tmp/motion-data-factory-preflight.json --markdown-output tmp/motion-data-factory-preflight.md`; `npm run lint`; `npm run build`; Playwright mobile smoke on `http://127.0.0.1:3016/motion-review`. | Plank review media now resolves to `/motion-review-assets/bodyweight_plank/mediapipe_trace_review.mp4` at 587,765 bytes, with source URL preserved. Local mobile proof selected Bodyweight Plank, confirmed the new video source, 574 KB label, detector failed, kinematic passed, visual review failed, Not validation-ready, and no horizontal overflow at 390 px. Counts remain 4 guide-ready, 0 validation-ready, 11 blocked. | Commit, push to `main`, deploy, and live-check the new review asset on Vercel. |
| 2026-06-25 15:26 CDT | Corrected the missing plank 3D motion demo by packaging the normalized plank JSONL and regenerating the website snapshot with embedded plank landmarks. | `python3 -m py_compile scripts/motion_reference/*.py`; focused motion-reference tests including `test_preflight_motion_data_factory.py`, `test_report_motion_pipeline_gaps.py`, `test_generate_motion_review_gallery_assets.py`, `test_score_motion_reference_trace.py`, and `test_register_motion_capture_session.py`; `python3 scripts/motion_reference/report_motion_pipeline_gaps.py`; `python3 scripts/motion_reference/preflight_motion_data_factory.py --json-output tmp/motion-data-factory-preflight.json --markdown-output tmp/motion-data-factory-preflight.md`; `npm run lint`; `npm run build`; Playwright mobile smoke on `http://127.0.0.1:3017/motion-review`. | Plank now has `Sources/CamiFitApp/Resources/MotionDemos/bodyweight_plank.jsonl`, 31 frames, 50 landmarks, and a nonblank 3D canvas. Playable traces increased to 5, while guide-ready remains 4, validation-ready remains 0, and plank remains `reference_capture_required` / `avatar-demo-candidate` with detector and visual-review blockers. | Commit, push to `main`, deploy, and live-check the plank 3D demo on Vercel. |
| 2026-06-25 15:39 CDT | Added a translucent capsule-mannequin overlay to the motion review canvas with icon toggles for mannequin volume and form-line cues. | `npm run lint`; `npm run build`; Playwright mobile smoke on `http://127.0.0.1:3018/motion-review`; saved screenshot `output/playwright/plank-mannequin-canvas-mobile.png`. | Plank renders with the raw detector skeleton still visible, translucent torso/limb/head/contact volumes overlaid, and a brighter plank alignment band. Browser proof confirmed the mannequin and form-line toggles materially change the canvas, no mobile overflow at 390 px, and plank remains not validation-ready with detector blockers visible. | Commit, push to `main`, deploy, and live-check the overlay on Vercel. |
| 2026-06-25 17:08 CDT | Packaged the next five review-only mannequin demos: Bodyweight Pike, Bodyweight Jumping Jack, Machine Chest-Supported Row, Suspension Tricep Press, and Single-Arm Dumbbell Preacher Curl. | `npm run lint`; `npm run build`; `python3 -m py_compile scripts/motion_reference/*.py`; focused motion-reference tests; `python3 scripts/motion_reference/report_motion_pipeline_gaps.py`; `python3 scripts/motion_reference/preflight_motion_data_factory.py --json-output tmp/motion-data-factory-preflight.json --markdown-output tmp/motion-data-factory-preflight.md`; Playwright mobile canvas proof on `http://127.0.0.1:3019/motion-review`. | Playable traces increased to 10 while guide-ready remained 4, validation-ready remained 0, and all 11 reference-capture-required exercises stayed blocked. The five new rows render nonblank 330x247 mobile canvases with mannequin/form-line controls and no horizontal overflow; manifests mark the traces as `motion_review_gallery_demo_only`. | Commit, push to `main`, deploy, then live-check the five new demos on Vercel. |
| 2026-06-25 17:31 CDT | Cleaned the Machine Chest-Supported Row review-only demo by replacing the noisy raw elbow phase with a single smoothstep pull-return envelope. | `npm run lint`; `npm run build`; `swift test --filter MachineChestSupportedRowAcceptanceTests`; focused motion-reference Python tests; `python3 scripts/motion_reference/report_motion_pipeline_gaps.py`; `python3 scripts/motion_reference/preflight_motion_data_factory.py --json-output tmp/motion-data-factory-preflight.json --markdown-output tmp/motion-data-factory-preflight.md`; Playwright mobile frame sweep on `http://127.0.0.1:3020/motion-review`. | The row's max phase jump dropped from `0.403749` to `0.036578`, and max rendered elbow/wrist step dropped from `0.097235` to `0.008809`. Browser proof on frames 48-55 showed a nonblank 330x247 canvas, no overflow, no app console errors, and the row remains not guide-ready/not validation-ready. | Commit, push to `main`, deploy, and live-check the cleaned row on Vercel. |
| 2026-06-25 18:20 CDT | Densified and smoothed the remaining review-only mannequin demos with a reproducible postprocess script: Bodyweight Jumping Jack, Bodyweight Pike, Suspension Tricep Press, and Single-Arm Dumbbell Preacher Curl. | `python3 -m py_compile scripts/motion_reference/*.py`; focused motion-reference tests including `test_smooth_review_demo_trace.py` and `test_update_motion_review_snapshot_traces.py`; `python3 scripts/motion_reference/report_motion_pipeline_gaps.py`; `python3 scripts/motion_reference/preflight_motion_data_factory.py --json-output tmp/motion-data-factory-preflight.json --markdown-output tmp/motion-data-factory-preflight.md`; `npm run lint`; `npm run build`; Swift focused acceptance/timeline/avatar tests; Playwright mobile proof on `http://127.0.0.1:3022/motion-review`. | Worst visible-step metrics improved without promotion: jumping-jack max landmark step `0.121929 -> 0.030482`, pike head/landmark pop `0.044602 -> 0.017932`, suspension wrist jump `0.290589 -> 0.052972`, preacher wrist jump `0.172899 -> 0.03458`. Snapshot has 10 playable traces and 10 detector-review videos while guide-ready remains 4 and validation-ready remains 0. Mobile screenshots saved under `output/playwright/motion-review-*-mobile.png`. | Commit, push to `main`, deploy, and live-check Vercel. |

## Stop Conditions

- Objective complete:
  Done criteria pass and proof target commands are green.
- Blocked:
  The same missing external input, capture asset, credential, or unavailable tool blocks progress for three materially identical iterations.
- Owner decision needed:
  A product decision is required between multiple incompatible data-source strategies, licensing obligations, or promotion policy changes.
- Cost/risk threshold:
  Work would require downloading large datasets, committing large artifacts, altering guide-ready app motion files, or changing installed-app behavior without a separate approval.
